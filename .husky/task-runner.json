{
   "tasks": [
      {
         "name": "fantomas-format",
         "group": "pre-commit",
         "command": "dotnet",
         "args": [ "fantomas", "${staged}", "--out", "${staged}" ],
         "include": ["**/*.fs", "**/*.fsi", "**/*.fsx"]
      }
   ]
}
